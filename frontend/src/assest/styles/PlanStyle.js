import { appBarHeight } from './AppStyle';

const style = {
    map: {
        //position: 'absolute',
        height: `calc(100vh - ${appBarHeight}px)`,
        width: '100%',
    },
};

export default style;